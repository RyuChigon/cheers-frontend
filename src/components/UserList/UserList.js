import React, {Component } from "react"
import { useHistory } from "react-router"
import {
  UserListContainer
} from './styled'

const UserList = () => {
    return(
        <UserListContainer>
            UserList
        </UserListContainer>
    )
}

export default UserList